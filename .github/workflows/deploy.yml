# This workflow is DISABLED.
# The app is a Next.js + Prisma app and cannot be deployed to GitHub Pages.
# Deployment is handled via Vercel.
#
# To re-enable, uncomment the trigger below and update the build steps.

name: Deploy (Disabled)

on:
  # Disabled — push to Vercel instead
  workflow_dispatch:

jobs:
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - name: Notice
        run: echo "Deployment is handled by Vercel. This workflow is disabled."
